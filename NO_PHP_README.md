# نظام تسجيل الدخول بدون PHP

## ✅ تم التحويل بنجاح!

النظام الآن يعمل **بدون PHP** - يستخدم JavaScript و localStorage فقط.

## 🚀 كيفية الاستخدام

### لا حاجة لخادم!
يمكنك فتح الملفات مباشرة من المتصفح:
- افتح `login.html` مباشرة من File Explorer
- أو استخدم أي خادم محلي (اختياري)

## 📁 الملفات الجديدة

### JavaScript Files:
- `js/auth-storage.js` - نظام التخزين والمصادقة
- `js/admin-storage.js` - وظائف إدارة الأعضاء
- `js/auth.js` - واجهة تسجيل الدخول (محدث)
- `js/admin.js` - لوحة التحكم (محدث)

### البيانات:
- البيانات تُحفظ في **localStorage** في المتصفح
- ملف `data/members.json` يُستخدم كنسخة احتياطية فقط

## 🔑 بيانات تسجيل الدخول

- **البريد الإلكتروني:** `admin@comixdev.com`
- **كلمة المرور:** `00100kayo`

## ✨ المميزات

1. ✅ **لا حاجة لـ PHP** - يعمل مباشرة من المتصفح
2. ✅ **لا حاجة لخادم** - افتح الملفات مباشرة
3. ✅ **localStorage** - البيانات تُحفظ في المتصفح
4. ✅ **نفس الوظائف** - إضافة، تعديل، حذف الأعضاء
5. ✅ **نظام صلاحيات** - يعمل بنفس الطريقة

## 📝 ملاحظات مهمة

### البيانات:
- البيانات تُحفظ في **localStorage** لكل متصفح
- إذا مسحت بيانات المتصفح، ستفقد البيانات
- يمكنك تصدير/استيراد البيانات من لوحة التحكم (قريباً)

### الأمان:
- ⚠️ **ملاحظة:** هذا النظام مناسب للاستخدام المحلي/التطوير
- كلمات المرور مشفرة بشكل بسيط (ليس آمن للاستخدام في الإنتاج)
- للاستخدام في الإنتاج، استخدم PHP أو خادم حقيقي

### الاستخدام:
- افتح `login.html` مباشرة
- سجّل الدخول بالبيانات أعلاه
- أضف/عدّل/احذف الأعضاء من لوحة التحكم

## 🔄 إذا أردت العودة لـ PHP:

1. أعد ملفات PHP من النسخة الاحتياطية
2. أزل `auth-storage.js` و `admin-storage.js`
3. استعد `auth.js` و `admin.js` القديمين

## 🎉 جاهز للاستخدام!

افتح `login.html` وابدأ الاستخدام مباشرة!

