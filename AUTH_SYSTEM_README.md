# نظام تسجيل الدخول وإدارة العضوية

## 📋 نظرة عامة

نظام كامل لتسجيل الدخول وإدارة الأعضاء مع نظام صلاحيات متقدم.

## 🔑 بيانات تسجيل الدخول الافتراضية

**البريد الإلكتروني:** `admin@comixdev.com`  
**كلمة المرور:** `password`

> **ملاحظة:** كلمة المرور المشفرة في الملف هي لكلمة "password". يمكنك تغييرها من لوحة التحكم بعد تسجيل الدخول.

## 📁 الملفات المُنشأة

### الصفحات
- `login.html` - صفحة تسجيل الدخول
- `admin/dashboard.html` - لوحة التحكم

### API Endpoints
- `api/login.php` - تسجيل الدخول
- `api/logout.php` - تسجيل الخروج
- `api/check-auth.php` - التحقق من الجلسة
- `api/members.php` - إدارة الأعضاء (GET, POST, PUT, DELETE)

### JavaScript
- `js/auth.js` - إدارة المصادقة والجلسات
- `js/admin.js` - وظائف لوحة التحكم

### CSS
- `css/auth.css` - أنماط صفحات المصادقة
- `css/admin.css` - أنماط لوحة التحكم

### البيانات
- `data/members.json` - قاعدة بيانات الأعضاء

## 🚀 كيفية الاستخدام

### 1. تسجيل الدخول
1. افتح `login.html` في المتصفح
2. أدخل البريد الإلكتروني وكلمة المرور
3. اضغط "تسجيل الدخول"

### 2. إضافة عضو جديد
1. بعد تسجيل الدخول، انتقل إلى "إضافة عضو جديد"
2. املأ البيانات:
   - الاسم الكامل
   - البريد الإلكتروني
   - كلمة المرور (6 أحرف على الأقل)
   - الصلاحية (عضو عادي / محرر / مدير)
   - الصلاحيات الإضافية (اختياري)
3. اضغط "إضافة عضو"

### 3. تعديل عضو
1. من صفحة "إدارة الأعضاء"
2. اضغط "تعديل" بجانب العضو المطلوب
3. عدّل البيانات
4. اضغط "حفظ التغييرات"

### 4. حذف عضو
1. من صفحة "إدارة الأعضاء"
2. اضغط "حذف" بجانب العضو المطلوب
3. أكد الحذف

## 🔐 أنواع الصلاحيات

### الأدوار (Roles)
- **عضو عادي (member):** عرض المحتوى فقط
- **محرر (editor):** عرض وتعديل المحتوى
- **مدير (admin):** جميع الصلاحيات

### الصلاحيات الإضافية
- **عرض المحتوى (view_content):** إمكانية عرض جميع المحتويات
- **تعديل المحتوى (edit_content):** إمكانية تعديل المحتويات الموجودة
- **حذف المحتوى (delete_content):** إمكانية حذف المحتويات
- **إدارة المستخدمين (manage_users):** إضافة وتعديل وحذف المستخدمين
- **إدارة الإعدادات (manage_settings):** تعديل إعدادات الموقع

## 🛡️ الأمان

- كلمات المرور مشفرة باستخدام `password_hash()` في PHP
- الجلسات (Sessions) محمية
- التحقق من الصلاحيات قبل الوصول للوظائف الحساسة
- حماية من CSRF (يمكن إضافة token لاحقاً)

## 📝 ملاحظات مهمة

1. **تأكد من صلاحيات الملفات:**
   - `data/members.json` يجب أن يكون قابل للكتابة (chmod 666 أو 644)

2. **الخادم:**
   - يتطلب PHP 7.0 أو أحدث
   - يجب تفعيل Sessions في PHP

3. **الأمان:**
   - غير كلمة المرور الافتراضية فوراً
   - استخدم HTTPS في الإنتاج
   - راجع صلاحيات الملفات بانتظام

## 🔧 التخصيص

### تغيير كلمة مرور المدير الافتراضية

1. افتح `data/members.json`
2. ابحث عن المدير الافتراضي
3. استخدم PHP لتشفير كلمة المرور الجديدة:
   ```php
   <?php
   echo password_hash('كلمة_المرور_الجديدة', PASSWORD_DEFAULT);
   ?>
   ```
4. استبدل قيمة `password` في الملف

### إضافة صلاحيات جديدة

1. عدّل `admin/dashboard.html` لإضافة checkbox جديد
2. عدّل `api/members.php` للتحقق من الصلاحية الجديدة
3. عدّل `js/admin.js` لعرض الصلاحية الجديدة

## 🐛 استكشاف الأخطاء

### لا يمكن تسجيل الدخول
- تأكد من أن PHP يعمل بشكل صحيح
- تحقق من صلاحيات ملف `data/members.json`
- راجع console المتصفح للأخطاء

### لا تظهر الأعضاء
- تحقق من أن المستخدم لديه صلاحية `manage_users`
- تأكد من أن API يعمل بشكل صحيح
- راجع Network tab في Developer Tools

### خطأ في حفظ البيانات
- تحقق من صلاحيات الكتابة على `data/members.json`
- تأكد من أن المجلد `data` موجود

## 📞 الدعم

إذا واجهت أي مشاكل، تحقق من:
1. Console المتصفح (F12)
2. Network tab للأخطاء في API
3. ملفات log في الخادم

---

**تم التطوير بواسطة:** ComixDev  
**التاريخ:** 2024

